import React, { FC } from 'react';
import { Card } from 'antd';
import Reddit from './App/Container';

const AMA: FC = () => {
  return (
    <Card>
      <Reddit />
    </Card>
  );
};

export default AMA;
